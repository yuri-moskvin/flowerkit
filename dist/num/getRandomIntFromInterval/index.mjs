const getRandomIntFromInterval=(min=1,max=1_000_000_000)=>{if(!Number.isFinite(min)||!Number.isFinite(max))throw new TypeError("getRandomIntFromInterval: min and max must be finite numbers");if(max<min){const t=min;min=max;max=t}min=Math.ceil(min);max=Math.floor(max);const span=max-min+1;if(!Number.isFinite(span)||span<=0)throw new RangeError("getRandomIntFromInterval: interval must contain at least one integer");return Math.floor(Math.random()*span+min)};export{getRandomIntFromInterval};
//# sourceMappingURL=index.mjs.map
