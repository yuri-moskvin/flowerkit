/**
 * Checks if the user has adblock
 * @return {boolean}
 * @example
 * // How to detect if user has adblock in browser?
 * const isAdblock = isAdblock();
 * console.log(isAdblock); // => false
 */
export function isAdblock(): boolean;
//# sourceMappingURL=index.d.ts.map