import{getDocument}from"ssr-window";const setCookie=(name,value,options={})=>{if(typeof name!=="string"||name.length===0)throw new TypeError("setCookie: name must be a non-empty string");if(typeof value!=="string")throw new TypeError("setCookie: value must be a string");if(options===null||typeof options!=="object"||Array.isArray(options))throw new TypeError("setCookie: options must be an object");const normalizedOptions={...options};let expires=normalizedOptions.expires;if(typeof expires==="number"&&expires>0){const d=new Date;d.setTime(d.getTime()+expires*1000);expires=d;normalizedOptions.expires=d}if(expires&&typeof expires.toUTCString==="function")normalizedOptions.expires=expires.toUTCString();const encoded=encodeURIComponent(value);let cookie=`${name}=${encoded}`;for(const key in normalizedOptions){if(!Object.prototype.hasOwnProperty.call(normalizedOptions,key))continue;cookie+=`; ${key}`;const optVal=normalizedOptions[key];if(optVal!==true&&typeof optVal!=="undefined")cookie+=`=${String(optVal)}`}getDocument().cookie=cookie};export{setCookie};
//# sourceMappingURL=index.mjs.map
