Object.defineProperty(exports,"__esModule",{value:true});const getAsyncPool=async(arr,callback,concurrency=4)=>{if(!Array.isArray(arr))throw new TypeError("getAsyncPool: arr must be an array");if(typeof callback!=="function")throw new TypeError("getAsyncPool: callback must be a function");if(!Number.isInteger(concurrency)||concurrency<=0)throw new TypeError("getAsyncPool: concurrency must be a positive integer");const result=new Array(arr.length);let nextIndex=0;const worker=async()=>{while(nextIndex<arr.length){const index=nextIndex;nextIndex+=1;result[index]=await callback(arr[index],index,arr)}};const workers=Array.from({length:Math.min(concurrency,arr.length)},()=>worker());await Promise.all(workers);return result};exports.getAsyncPool=getAsyncPool;
//# sourceMappingURL=index.cjs.map
