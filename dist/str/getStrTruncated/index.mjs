const getStrTruncated=(str,maxLength,suffix="…")=>{if(typeof str!=="string")throw new TypeError("getStrTruncated: str must be a string");if(!Number.isInteger(maxLength)||maxLength<0)throw new TypeError("getStrTruncated: maxLength must be a non-negative integer");if(typeof suffix!=="string")throw new TypeError("getStrTruncated: suffix must be a string");const chars=Array.from(str);if(chars.length<=maxLength)return str;const suffixChars=Array.from(suffix);if(suffixChars.length>=maxLength)return suffixChars.slice(0,maxLength).join("");return chars.slice(0,maxLength-suffixChars.length).join("")+suffix};export{getStrTruncated};
//# sourceMappingURL=index.mjs.map
